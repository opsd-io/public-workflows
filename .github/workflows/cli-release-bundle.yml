name: CLI release bundle

on:
  workflow_call:
    inputs:
      build-script:
        description: Path to the portable bundle build script.
        required: false
        type: string
        default: scripts/release/build_portable_bundle.sh
      checkout-ref:
        description: Git ref to build. Defaults to the workflow ref.
        required: false
        type: string
        default: ''
      release-tag:
        description: GitHub Release tag to receive the assets. Defaults to the workflow ref name.
        required: false
        type: string
        default: ''
      linux-runner:
        description: JSON array of labels for the Linux runner.
        required: false
        type: string
        default: '["self-hosted", "Linux", "X64"]'
      macos-runner:
        description: JSON array of labels for the macOS runner.
        required: false
        type: string
        default: '["self-hosted", "macOS", "ARM64"]'

permissions:
  contents: write

concurrency:
  group: cli-release-bundle-${{ github.repository }}-${{ github.ref_name }}
  cancel-in-progress: false

env:
  GH_TOKEN: ${{ github.token }}

jobs:
  linux:
    name: Linux x64 bundle
    runs-on: ${{ fromJSON(inputs.linux-runner) }}
    timeout-minutes: 30
    steps:
      - name: Check out tagged source
        uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
        with:
          ref: ${{ inputs.checkout-ref || github.ref }}
      - name: Set up Ruby with rbenv
        shell: bash
        run: |
          export RBENV_ROOT="${RBENV_ROOT:-${HOME}/.rbenv}"
          export PATH="${RBENV_ROOT}/bin:${PATH}"
          command -v rbenv >/dev/null || {
            echo "rbenv is required on the Linux self-hosted runner" >&2
            exit 1
          }
          eval "$(rbenv init - bash)"
          ruby_version="$(tr -d '[:space:]' < .ruby-version)"
          rbenv install -s "${ruby_version}"
          rbenv shell "${ruby_version}"
          ruby_bin="$(rbenv which ruby)"
          test "$(${ruby_bin} --disable-gems -e 'print RUBY_VERSION')" = "${ruby_version}"
          echo "OPSD_RUBY_BIN=${ruby_bin}" >> "${GITHUB_ENV}"
      - name: Run unit tests
        run: |
          "${OPSD_RUBY_BIN}" -Ilib:test -e 'Dir["test/*_test.rb"].sort.each { |f| load f }'
      - name: Build bundle
        run: ${{ inputs.build-script }}
      - name: Generate checksum
        shell: bash
        run: |
          bundle=$(find dist -maxdepth 1 -name '*.tar.gz' -print -quit)
          bundle_name=${bundle##*/}
          sha256sum "$bundle" > "dist/${bundle_name}.sha256"
      - name: Wait for GitHub Release
        shell: bash
        env:
          RELEASE_TAG: ${{ inputs.release-tag || github.ref_name }}
        run: |
          attempt=1
          while ! gh release view "${RELEASE_TAG}" >/dev/null 2>&1 && [ "$attempt" -le 12 ]; do
            attempt=$((attempt + 1))
            sleep 5
          done
          gh release view "${RELEASE_TAG}" >/dev/null 2>&1 && exit 0
          echo "GitHub Release ${RELEASE_TAG} was not found" >&2
          exit 1
      - name: Upload release assets
        run: gh release upload "${{ inputs.release-tag || github.ref_name }}" dist/*.tar.gz dist/*.sha256 --clobber

  macos:
    name: macOS ARM64 bundle
    runs-on: ${{ fromJSON(inputs.macos-runner) }}
    timeout-minutes: 30
    steps:
      - name: Check out tagged source
        uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
        with:
          ref: ${{ inputs.checkout-ref || github.ref }}
      - name: Verify runner Ruby
        shell: bash
        env:
          OPSD_MACOS_RUBY_BIN: ${{ vars.OPSD_MACOS_RUBY_BIN }}
        run: |
          test -n "${OPSD_MACOS_RUBY_BIN}" || {
            echo "OPSD_MACOS_RUBY_BIN repository or organization variable is required" >&2
            exit 1
          }
          test -x "${OPSD_MACOS_RUBY_BIN}" || {
            echo "Ruby executable does not exist: ${OPSD_MACOS_RUBY_BIN}" >&2
            exit 1
          }
          test "$(${OPSD_MACOS_RUBY_BIN} --disable-gems -e 'print RUBY_VERSION')" = "$(tr -d '[:space:]' < .ruby-version)"
      - name: Run unit tests
        env:
          OPSD_MACOS_RUBY_BIN: ${{ vars.OPSD_MACOS_RUBY_BIN }}
        run: |
          "${OPSD_MACOS_RUBY_BIN}" -Ilib:test -e 'Dir["test/*_test.rb"].sort.each { |f| load f }'
      - name: Build bundle
        env:
          OPSD_RUBY_BIN: ${{ vars.OPSD_MACOS_RUBY_BIN }}
        run: ${{ inputs.build-script }}
      - name: Generate checksum
        shell: bash
        run: |
          bundle=$(find dist -maxdepth 1 -name '*.tar.gz' -print -quit)
          bundle_name=${bundle##*/}
          shasum -a 256 "$bundle" > "dist/${bundle_name}.sha256"
      - name: Wait for GitHub Release
        shell: bash
        env:
          RELEASE_TAG: ${{ inputs.release-tag || github.ref_name }}
        run: |
          attempt=1
          while ! gh release view "${RELEASE_TAG}" >/dev/null 2>&1 && [ "$attempt" -le 12 ]; do
            attempt=$((attempt + 1))
            sleep 5
          done
          gh release view "${RELEASE_TAG}" >/dev/null 2>&1 && exit 0
          echo "GitHub Release ${RELEASE_TAG} was not found" >&2
          exit 1
      - name: Upload release assets
        run: gh release upload "${{ inputs.release-tag || github.ref_name }}" dist/*.tar.gz dist/*.sha256 --clobber
